[Controls]

bind ^ console
bind 1 rendermode default

bind 2 rendermode bounding
bind 3 rendermode editor
bind 4 rendermode profile

bind w forward

bind a left
bind s back

bind d right
bind 0x09 toggleCamera

[most of them only work in editmode]
bind e pick
bind q spotlight
bind t spawn box.obj box
bind z spawn sphere.obj sphere
bind y spawn sphere.obj sphere
bind p physical
bind f flushVR
bind r removepicked
bind k down scalesmall
bind 0x20 jump 3
bind l down scalebig
bind v pforce
bind 0 rasterstate wire
bind 9 rasterstate frontfaces
bind 5 fullscreen 1
bind 6 fullscreen 0
bind u down rotateX 0.1
bind i down rotateY 0.1
bind o toggleRotationDir
bind 7 spawn spot.obj sphere
bind g sl

