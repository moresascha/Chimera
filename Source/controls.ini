[Controls]

bind ^ console
[Rendering 0=most quality and fastes mode, 1=boundingboxes, 2=editmode]
bind 1 rendermode default

bind 2 rendermode debug
bind 3 rendermode editor
bind 4 rendermode profile

bind w forward

bind a left
bind s back

bind d right
bind 0x09 toggleCamera

bind u runscript test.lua

[most of them only work in editmode]
bind e pick
bind q spotlight
bind t spawn box.obj box
bind z spawn sphere.obj sphere
bind y spawn sphere.obj sphere
bind p physical
bind f flushVR
bind r removepicked
bind o playsound ineption_horn_HIGH.wav
bind k scalesmall
bind 0x20 jump 2.5
bind l scalebig
bind v pforce
bind j runproc nvcc -arch=sm_10 --use-local-env --cl-version 2010 -I"C:\Program Files (x86)\Microsoft Visual Studio 10.0\VC\include" -I"C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v4.2\include" -I"../include" -G  --keep-dir "x64\Debug" -maxrregcount=0  --machine 64 -ptx  -o "files/shader/particle.ptx" "E:\Dropbox\Visual Studio\shrUtils\shrUtils\particles.cu"
bind 0 rasterstate wire
bind 9 rasterstate frontfaces


